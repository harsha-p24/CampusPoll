# CampusPoll — Nginx reverse proxy config
# Place at: /etc/nginx/sites-available/campuspoll
# Enable: ln -s /etc/nginx/sites-available/campuspoll /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # Redirect all HTTP to HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL — certificates from Let's Encrypt (certbot)
    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Security headers
    add_header X-Frame-Options          DENY;
    add_header X-Content-Type-Options   nosniff;
    add_header X-XSS-Protection         "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Referrer-Policy          "strict-origin-when-cross-origin";

    # Static files — served directly by Nginx (no Flask overhead)
    location /static/ {
        alias /var/www/campuspoll/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Uploaded images
    location /static/images/ {
        alias /var/www/campuspoll/static/images/;
        expires 7d;
    }

    # All other requests → Gunicorn
    location / {
        proxy_pass         http://127.0.0.1:5000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
        client_max_body_size 2M;
    }

    # Logging
    access_log /var/log/nginx/campuspoll_access.log;
    error_log  /var/log/nginx/campuspoll_error.log;
}
